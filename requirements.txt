# AradVision 基础依赖（测试基线）
# 说明：
# 1. 先安装这些依赖，确保单元测试可以运行；
# 2. 视觉与系统集成依赖可在下一阶段按模块补充。

numpy>=1.24.0
pytest>=7.4.0
pytest-cov>=4.1.0
PyYAML>=6.0
loguru>=0.7.0

# 输入与熔断相关依赖（真实环境）
pydirectinput>=1.0.4
keyboard>=0.13.5
