# 工作总结：固定路线文档收口与项目说明

- 日期：2026-03-14
- 角色：架构与逻辑工程师（Codex）

## 本次提交概述

本次提交主要完成两类工作：

1. 固定路线 MVP 主链路代码与配置收口，包括 `FixedRoutePipeline`、Reader、守护层、路线控制器、维护控制器及其测试接入。
2. 文档收口，新增项目整体情况说明书，删除不再保留的《歼灭追击战固定路线 MVP 资料收集模板》，同步更新文档索引。

## 主要改动

1. 新增固定路线运行主链路相关模块：
   - `logic/fixed_route_pipeline.py`
   - `logic/guard_layer.py`
   - `logic/route_run_controller.py`
   - `logic/maintenance_controller.py`
   - `vision/main_view_reader.py`
   - `vision/minimap_reader.py`
   - `vision/state_reader.py`
2. 更新主程序与 UI 线程，使控制台模式和 UI 模式都走 `FixedRoutePipeline`。
3. 扩展 `core/config.py`、`core/types.py` 与 `configs/config.yaml`，补齐 `dungeon_run` 运行配置和上下文结构。
4. 增加固定路线相关测试：
   - `tests/test_config_dungeon_run.py`
   - `tests/test_dungeon_run_pipeline.py`
5. 新增项目说明文档：
   - `docs/项目整体情况说明书/项目整体情况说明书.md`
6. 从固定路线专题中移除 `资料收集模板.md` 及其引用，避免继续维护目标不明确的资料模板。

## 验证情况

已执行：

```bash
pytest -q tests/test_config_dungeon_run.py tests/test_dungeon_run_pipeline.py tests/integration/test_full_flow_mock.py
pytest -q tests
pytest -q
python3 -m compileall core input logic vision ui tests main.py scripts
```

结果摘要：

1. 固定路线主线相关定向测试通过。
2. `tests/` 目录仍有 3 个历史失败用例，位于 `coordinate_mapper` 和 `input_driver`。
3. 全量 `pytest -q` 在当前环境因缺少 `PyQt5` 无法完成。
4. `compileall` 仍被 `scripts/label_guide.py` 的历史语法错误阻断。

## 风险与后续建议

1. 真实《歼灭追击战》路线脚本与 ROI 仍需结合实机继续复核。
2. `dungeon_run.enabled` 尚未真正成为主链路开关。
3. UI 依赖和历史脚本问题仍需继续清理，才能形成更干净的仓库级质量基线。
