# 测试记录

## 执行日期

2026-03-14

## 已执行命令

### 1. 语法验证

```bash
python3 -m compileall core input logic vision ui tests main.py
```

结果：

- 通过

### 2. 变更相关测试

```bash
python3 -m pytest -q tests/test_types.py tests/test_config_capture_backend.py tests/test_config_dungeon_run.py tests/test_dungeon_run_pipeline.py tests/test_bot_fsm.py tests/test_world_model.py tests/integration/test_full_flow_mock.py
```

结果：

- `52 passed`

### 3. 仓库正式测试目录

```bash
python3 -m pytest -q tests/
```

结果：

- 大部分通过
- 当前剩余 `3` 个失败，均为历史模块问题，未由本专题引入：
  - `tests/test_coordinate_mapper.py::TestCoordinateMapper::test_is_aligned_custom_tolerance`
  - `tests/test_coordinate_mapper.py::TestCoordinateMapper::test_get_x_direction`
  - `tests/test_input_driver.py::test_invalid_key_should_raise`

### 4. 全量 pytest

```bash
python3 -m pytest -q
```

结果：

- 收集阶段失败
- 原因：`scripts/test_pyqt_ui.py`、`scripts/test_timeline_ui.py` 依赖本机未安装的 `PyQt5`
- 该问题与本专题实现无直接关系

## 结论

- 新增固定路线 MVP 主链路已通过语法检查
- 与本次改动直接相关的测试全部通过
- 仓库仍存在少量历史失败项与本机 UI 依赖缺失问题，需后续单独处理
