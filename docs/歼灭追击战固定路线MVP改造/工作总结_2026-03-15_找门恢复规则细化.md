# 工作总结：找门恢复规则细化

- 日期：2026-03-15
- 角色：架构与逻辑工程师（Codex）

## 本次提交概述

本次提交继续在“固定路线找门与纠偏”专题下细化恢复机制文档，不改动代码。重点是把“跳跃作为最后兜底”“四方向对称恢复规则”和“首版阈值建议”补成明确、可实现的文档口径。

## 主要改动

1. 更新《固定路线找门与纠偏设计》，明确恢复态退出奔跑、改用短步探测。
2. 新增四方向对称恢复规则表：
   - `RIGHT` 被卡住：`UP -> DOWN -> LEFT -> JUMP`
   - `LEFT` 被卡住：`UP -> DOWN -> RIGHT -> JUMP`
   - `UP` 被卡住：`RIGHT -> LEFT -> DOWN -> JUMP`
   - `DOWN` 被卡住：`RIGHT -> LEFT -> UP -> JUMP`
3. 新增首版阈值建议，包括卡住判定窗口、主方向最小有效位移、探测步长、回退步长和最大恢复轮数。
4. 更新主专题文档与项目整体说明，把“绕行优先、跳跃最后”的恢复口径同步到主线文档。

## 验证情况

已执行：

```bash
git diff --check -- docs/项目整体情况说明书/项目整体情况说明书.md docs/歼灭追击战固定路线MVP改造/歼灭追击战固定路线MVP改造.md docs/歼灭追击战固定路线MVP改造/固定路线找门与纠偏设计.md docs/歼灭追击战固定路线MVP改造/工作总结_2026-03-15_找门恢复规则细化.md
```

结果：

1. 本次文档补充未引入空白符或补丁格式问题。

## 当前边界

1. 这次仍是文档细化，不包含任何状态机、配置字段或检测器实现改动。
2. 文档中的阈值是首版建议值，后续仍需结合《歼灭追击战》实机录像与运行日志微调。
3. 跳跃被明确降级为最后兜底，但不代表所有平台房都能完全不使用跳跃；具体触发仍需结合房间地形实现。
