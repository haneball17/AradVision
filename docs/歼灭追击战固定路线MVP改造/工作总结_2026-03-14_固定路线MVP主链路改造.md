# 工作总结 2026-03-14 固定路线MVP主链路改造

## 本轮完成

- 在 `core/types.py` 中补齐固定路线 MVP 所需状态层和上下文字段
- 在 `core/config.py` / `configs/config.yaml` 中新增 `dungeon_run` 配置结构
- 新增主画面、小地图、UI 状态三个 Reader
- 新增守护层、固定路线控制器、维护流程控制器
- 新增 `FixedRoutePipeline`，并接入 `main.py` 与 `ui/threads/engine_thread.py`
- 新增配置解析和固定路线主流程测试

## 关键取舍

- 保留旧 `WorldModel/BotFSM/PathPlanner`，但不再作为《歼灭追击战》默认主链路
- `YOLO 通用方案` 只保留为后续升级目标，不在当前专题展开
- 维护流程首版只做 dry run，不直接执行真实卖店

## 当前收益

- 项目默认运行路径已经从“通用对象检测叙事”切换到“固定路线状态驱动叙事”
- 后续如果要接 YOLO 增强，只需挂接 Reader，不必推翻当前控制器边界
- 配置、测试、文档口径已和《歼灭追击战》MVP 目标对齐

## 当前不足

- 房间脚本仍是结构示例，缺少真实逐房间实测数据
- ROI 仍需按真实窗口与 HUD 做现场标定
- 历史测试中仍存在 `coordinate_mapper` 和 `input_driver` 的遗留失败项
