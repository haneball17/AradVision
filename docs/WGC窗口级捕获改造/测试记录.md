# 测试记录

- 主题：WGC窗口级捕获改造
- 日期：2026-02-18
- 角色：架构与逻辑工程师（Codex）

## 1. 自动化检查

### 1.1 目标文件语法检查（通过）

执行命令：

```bash
python3 -m py_compile \
  core/capture.py \
  core/config.py \
  main.py \
  ui/threads/engine_thread.py \
  tests/test_config_capture_backend.py \
  tests/test_capture_factory.py \
  scripts/verify_capture_backend.py
```

结果：通过。

### 1.2 全量 compileall（存在历史遗留失败）

执行命令：

```bash
python3 -m compileall core ui tests scripts main.py
```

结果：失败（非本次改动引入）。

失败文件：
- `scripts/label_guide.py`（历史语法错误）
- `scripts/test_input_real.py`（历史语法错误）

说明：本次变更相关文件不在上述失败列表内，且已通过 1.1 的定向语法校验。

## 2. 单元测试状态

- 新增测试文件：
  - `tests/test_config_capture_backend.py`
  - `tests/test_capture_factory.py`
- 当前环境未执行 `pytest` 完整回归（依赖环境受限）。

建议在具备测试依赖的环境执行：

```bash
python3 -m pytest tests/test_config_capture_backend.py -q
python3 -m pytest tests/test_capture_factory.py -q
python3 -m pytest -q
```

## 3. 人工验证建议（Windows）

使用脚本：`scripts/verify_capture_backend.py`

示例：

```bash
python3 scripts/verify_capture_backend.py --backend auto --window-title "DNF Taiwan" --frames 120
python3 scripts/verify_capture_backend.py --backend wgc --window-title "DNF Taiwan" --frames 120
python3 scripts/verify_capture_backend.py --backend mss --window-title "DNF Taiwan" --frames 120
```

关注点：
1. 日志中的 `requested_backend/active_backend` 是否符合预期。  
2. 遮挡场景下 WGC 与 MSS 输出帧差异。  
3. WGC 初始化失败时是否自动降级且主流程不中断。
