# 工作总结：C++项目协作接口协议V6

- 日期：2026-03-12
- 主题：C++项目协作接口协议V6

## 本次提交内容

- 纳入 `AradVision` 与 `game-all` 协作接口协议文档
- 明确 `HelperStatus V6` 与 `HelperControl V5` 的通信方向
- 固定共享内存命名、结构体大小、字段偏移和技术约束

## 协议重点

- `game-all` 通过 `HelperStatusV6` 向 `AradVision` 提供房间清空、门开启、怪物数量和过图就绪状态
- `AradVision` 通过 `HelperControlV5` 向 `game-all` 发送上、下、左、右过图控制指令
- 双方采用 Windows 命名共享内存、小端序和 `Pack = 1` 作为兼容基线

## 当前结论

- 本次为文档提交，不包含 Python 或 C++ 侧实现代码
- 文档已给出 C++ `static_assert` 与 Python `ctypes.Structure` 对齐基线
- 运行时联调和共享内存读写验证需在后续接入阶段完成
