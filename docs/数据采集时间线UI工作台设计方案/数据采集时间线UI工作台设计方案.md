# 数据采集时间线UI工作台设计方案

**文档主题**: 数据采集时间线UI工作台设计方案  
**文档路径**: `docs/数据采集时间线UI工作台设计方案/数据采集时间线UI工作台设计方案.md`  
**版本**: v1.3  
**日期**: 2026-02-25  
**角色**: 架构与逻辑工程师（yangmq17）

## 1. 需求审核（Code Prompt Coach）
1. 清晰性：目标明确，核心是“按时间线浏览并按区间抽样导出”。
2. 具体性：已给出关键功能（任意时间区间、采样频率）。
3. 上下文：与现有 PyQt5 UI、`samples.jsonl` 元数据体系可直接衔接。
4. 结构化补充（本方案默认）：首版以桌面端单窗口工具为主，不引入新前端框架。

## 2. 设计目标
1. 30 秒内完成一次“区间选择 + 频率抽样 + 导出”。
2. 对错误操作有明显防呆（区间无效、导出量过大、磁盘空间不足）。
3. 与现有主题、日志、配置机制保持一致，开发成本可控。
4. 支持 1 万张级别样本浏览，主线程不阻塞。
5. 提供采集控制入口，首版支持直接“开始/停止”采集。

## 3. 信息架构
1. 左侧：会话与场景筛选。
2. 中央：时间线轨道 + 缩略图流。
3. 右侧：导出参数与统计结果。
4. 顶部：采集控制条（开始/停止，P1 增加暂停/继续）。
5. 底部：日志与状态栏。

## 4. 低保真草图（桌面主界面）
```text
┌──────────────────────── 数据采集时间线工作台 ────────────────────────┐
│ 采集: [开始] [停止] (P1:[暂停/继续])  状态: 空闲                        │
│ 会话: [day1_luolan ▼]  场景: [全部 ▼]  搜索: [__________]  [刷新]       │
├───────────────┬───────────────────────────────────────┬───────────────┤
│ 会话列表       │ 时间线轨道（可缩放/拖拽）              │ 导出参数       │
│ - day1_a      │ 10:00 ────●────●──A═══════B──●── 10:30 │ 起点 A [10:08] │
│ - day1_b      │               ↑当前帧                  │ 终点 B [10:16] │
│ - day2_test   │                                       │ 频率 [1s ▼]    │
│               ├──────────────── 缩略图时间流 ──────────┤ 仅保留清晰 [√] │
│               │ [img][img][img][img][img][img][img]   │ 去重过滤   [√] │
│               │ [img][img][img][img][img][img][img]   │ 场景过滤 [全部]│
│               │ 点击预览大图 / Shift多选 / 双击定位时间 │ 预计导出: 168  │
├───────────────┴───────────────────────────────────────┼───────────────┤
│ 日志: 已加载 1024 张，当前区间 10:08:12 - 10:16:24，过滤后 168 张         │
└───────────────────────────────────────────────────────┴───────────────┘
```

## 5. 关键交互草图（导出确认）
```text
┌────────────── 导出确认 ──────────────┐
│ 会话: day1_luolan                    │
│ 区间: 10:08:12 ~ 10:16:24            │
│ 采样频率: 1s/张                       │
│ 过滤: 清晰度>=80, 去重=开启           │
│ 预计导出: 168 张 (combat 90 / loot 30)│
│ 输出目录: assets/images/selected/...  │
│ [取消]                      [开始导出] │
└──────────────────────────────────────┘
```

## 6. 功能清单
### 6.1 必须功能（P0）
1. 采集控制入口：提供“开始/停止”按钮，直接拉起/停止采集脚本。
2. 采集状态展示：`空闲/采集中/停止中/异常`。
3. 时间线双锚点选择（A/B）。
4. 区间采样频率（1s、2s、5s、自定义）。
5. 导出预估数量与分布统计。
6. 导出结果与 `selection_manifest.json`。

### 6.2 增强功能（P1）
1. 质量过滤：`blur_score`、`similarity_to_prev`。
2. 场景筛选：`combat/navigate/loot/boss/other`。
3. 快捷键：`[` 设A，`]` 设B，`Ctrl+E` 导出。
4. 采集暂停/继续：在同一会话内暂停落盘并可继续，保持编号连续。

### 6.3 进阶功能（P2）
1. 区间批量修正场景标签。
2. 导出前后分布对比报告。

### 6.4 采集控制分级决策（新增）
1. `开始/停止` 归入 P0：直接决定是否能在 UI 内闭环完成“采集 -> 筛选 -> 导出”。
2. `暂停/继续` 归入 P1：有价值但非首版必需，避免 P0 过早引入状态机复杂度。
3. 暂停语义：停止新增落盘，不结束会话。
4. 继续语义：在同一会话续写，编号连续。
5. 停止语义：执行会话收尾并写入最终统计。

## 7. 技术实现建议（贴合现有项目）
1. UI 框架：复用 PyQt5（不新增 Web 依赖）。
2. 新增模块建议：
- `ui/widgets/timeline_panel.py`：时间线轨道与缩放逻辑。
- `ui/widgets/frame_strip.py`：缩略图时间流。
- `ui/widgets/export_panel.py`：区间/频率/过滤/导出。
- `ui/widgets/capture_control_bar.py`：采集控制条（开始/停止，P1 扩展暂停/继续）。
- `ui/timeline_window.py`：工作台主窗口。
3. 数据源：优先读取 `meta/samples.jsonl`，无元数据时回退文件名时间戳。
4. 输出规范：`assets/images/selected/<session>/<export_id>/`。
5. 进程控制建议：
- 首选拉起 `scripts/collect_yolo_data.py`（新采集主链路）。
- 兼容回退 `scripts/capture_training_data.py`（现有脚本：运行即开始，`F12` 停止）。

## 8. 验收标准
1. 1000 张样本加载时间 < 2 秒，滚动与拖拽无明显卡顿。
2. 5000 张样本加载时间 < 4 秒，时间线缩放无明显掉帧。
3. 10000 张样本首次可见区域渲染 < 2 秒，完整缩略图采用懒加载。
4. 任意 A/B 区间导出结果与预估误差为 0。
5. 导出吞吐不低于 100 张/秒（copy 模式，1080p jpg，SSD 环境）。
6. 导出清单包含参数、时间范围、过滤条件、样本数量。
7. 无效区间、异常路径、磁盘不足有明确错误提示。
8. 点击“开始”后 3 秒内进入采集中状态，点击“停止”后 5 秒内完成停采并写入会话收尾信息。
9. P1 场景下暂停后不再新增样本，继续后样本编号连续且写入同一会话。

## 9. 数据契约（新增）
### 9.1 `samples.jsonl` 字段规范
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `sample_id` | `string` | 是 | 样本唯一标识，建议 UUID 或稳定哈希。 |
| `timestamp_ms` | `int64` | 是 | 采样时间戳（毫秒），区间与抽样计算唯一基准。 |
| `timestamp_iso` | `string` | 是 | ISO8601 时间字符串，建议 UTC（如 `2026-02-25T06:08:12.123Z`）。 |
| `image_rel_path` | `string` | 是 | 相对会话根目录的图片路径。 |
| `scene` | `string` | 是 | `combat/navigate/loot/boss/other`。 |
| `backend` | `string` | 是 | `wgc/mss/mock`。 |
| `width` | `int` | 是 | 图片宽度。 |
| `height` | `int` | 是 | 图片高度。 |
| `filtered` | `bool` | 是 | 是否被过滤。 |
| `filter_reason` | `string|null` | 否 | `blur/duplicate/manual/none` 等。 |
| `blur_score` | `float|null` | 否 | 清晰度分数。 |
| `similarity_to_prev` | `float|null` | 否 | 与前帧相似度或差异指标。 |
| `window_state` | `string|null` | 否 | `foreground/background/minimized`。 |

示例（单行 JSON）：
```json
{"sample_id":"e7f1...","timestamp_ms":1772009292123,"timestamp_iso":"2026-02-25T06:08:12.123Z","image_rel_path":"images/combat_000123.jpg","scene":"combat","backend":"wgc","width":1920,"height":1080,"filtered":false}
```

### 9.2 `selection_manifest.json` 字段规范
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `schema_version` | `string` | 是 | 协议版本，如 `1.0`。 |
| `export_id` | `string` | 是 | 本次导出唯一 ID。 |
| `source_session` | `string` | 是 | 来源会话名。 |
| `created_at_iso` | `string` | 是 | 导出创建时间。 |
| `time_range` | `object` | 是 | `{start_ms,end_ms,inclusive}`。 |
| `sampling` | `object` | 是 | `{interval_ms,mode,tolerance_ms,missing_policy}`。 |
| `filters` | `object` | 是 | 场景/质量过滤条件。 |
| `export` | `object` | 是 | 输出目录、冲突策略、导出模式。 |
| `result` | `object` | 是 | 原始计数、导出计数、跳过计数、错误计数。 |

设计理由：把“导出行为参数”和“导出结果”一起持久化，确保可追溯、可复现、可审计。

## 10. 时间语义（新增）
1. 区间定义为闭区间 `[A, B]`，包含 A 与 B 对应样本。
2. 若用户输入 `A > B`，系统自动交换并提示。
3. 存储基准统一使用 `timestamp_ms`（UTC 毫秒）；`timestamp_iso` 仅用于展示与日志。
4. UI 显示默认使用系统本地时区，鼠标悬停同时显示 UTC。
5. 时间戳解析优先级：
- `timestamp_ms`
- `timestamp_iso`
- 文件名时间戳
- 文件 `mtime`

设计理由：用单一数值时间轴做计算，避免时区与字符串解析差异导致的不确定行为。

## 11. 抽样算法（新增）
1. 采用“时间网格 + 最近帧匹配”模式。
2. 计算网格点：`t_k = A + k * interval_ms`，直到 `t_k > B`。
3. 在窗口 `[-tolerance_ms, +tolerance_ms]` 内选择离 `t_k` 最近且未使用样本。
4. 默认 `tolerance_ms = min(0.4 * interval_ms, 200)`。
5. 若窗口内无样本，按 `missing_policy=skip` 跳过并累计 `missing_count`。
6. 导出结束后在 manifest 输出：`grid_count/selected_count/missing_count/duplicate_drop_count`。

设计理由：比严格命中更稳健，比简单最近邻更可控，且结果可复现。

## 12. 导出策略（新增）
1. 导出模式：默认 `copy`，可选 `hardlink`（仅同盘且系统支持时）。
2. 命名规则：导出文件统一顺序命名（`000001.jpg`），源路径映射写入 manifest。
3. 冲突策略：默认 `new_dir`（自动追加 `export_id`），可选 `error/overwrite`。
4. 重名处理：同次导出由顺序命名规避，不依赖原文件名去重。
5. 输出目录约定：`assets/images/selected/<session>/<export_id>/`。

设计理由：默认策略以安全性优先，避免误覆盖；高级策略按需开启。

## 13. 性能策略（新增）
1. 元数据与图像解码分离：主线程仅加载元数据，缩略图在后台线程解码。
2. 缩略图懒加载：仅渲染可视区与预加载窗口。
3. 预加载窗口：当前可视区前后各 200 张，可配置。
4. 缩略图缓存上限：默认 800 张，采用 LRU 逐出。
5. 时间线绘制降采样：单次渲染最多 2000 个点，超出部分聚合显示。
6. 并发解码线程：`min(4, CPU核心数-1)`，避免争抢 UI 线程。
7. 统计计算分批执行：每批 500 条，批间让出事件循环。

设计理由：控制主线程负载和内存峰值，保证 1 万张级别下可操作性。

## 14. 失败与回滚（新增）
1. 导出事务模型：先写入临时目录 `.tmp_export_<id>`，成功后原子重命名为正式目录。
2. 用户取消导出：停止后清理临时目录，并在日志记录已处理数量。
3. 磁盘不足：立即中止，保留 `errors.jsonl` 与失败原因，不生成正式导出目录。
4. 坏图像或读写异常：按 `on_error=skip` 跳过并记录，不中断整批导出。
5. 崩溃恢复：下次启动扫描残留临时目录，支持“清理/恢复”二选一。
6. 可选调试开关：`keep_failed_temp=true` 时保留临时目录用于排障。

设计理由：保证结果一致性，降低半成品污染风险，并保留足够排障线索。

## 15. 文档边界说明（新增）
1. 本文档只覆盖 UI 工作台能力：采集控制、时间线浏览、区间筛选、抽样导出、导出错误恢复。
2. 训练工程能力（数据集划分、防泄露、YOLO 格式校验、伪标签版本管理、尺寸分析）统一在：
- `docs/YOLO训练数据采集与伪标签方案/YOLO训练数据采集与伪标签方案.md`
3. 若 UI 仅作为训练工程能力入口，本文只保留入口交互，不重复定义训练算法与数据契约细节。
