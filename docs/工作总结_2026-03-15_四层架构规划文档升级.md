# 工作总结：四层架构规划文档升级

- 日期：2026-03-15
- 角色：架构与逻辑工程师（Codex）

## 本次提交概述

本次提交只涉及主线文档口径升级，不改动任何代码实现。核心目标是把项目的长期方向从“能力追加式描述”整理为“当前实现事实 + 四层目标架构 + 分阶段推进路线”，同时明确当前交付仍然是《歼灭追击战》固定路线 MVP。

## 主要改动

1. 重写《架构设计文档》的主口径，新增四层目标架构、双链路原则和跨游戏复用约束。
2. 更新《项目整体情况说明书》，将后续路线整理为 `P0 ~ P4` 的分阶段推进方案。
3. 更新 `docs/README.md` 导航摘要，明确多副本双链路、跨游戏复用和 LLM 决策层属于后续规划，不是当前已实现事实。

## 当前确认的关键口径

1. 当前主线仍是《歼灭追击战》固定路线 MVP，默认运行链路没有改变。
2. 中期目标是“多副本双链路”，已适配副本继续走固定路线，未适配副本再逐步接入通用 YOLO。
3. 长期目标是“LLM 高层决策层”，但 LLM 只负责高层任务和策略，不直接控制底层键鼠。
4. 跨游戏复用通过新增游戏适配层实现，不采用推倒式重写当前 DNF 资产。

## 验证与风险

1. 本次为 Markdown 文档改动，未运行代码测试。
2. 当前仓库仍有既有的测试失败、PyQt5 依赖缺失和历史脚本语法问题，本次提交不处理这些问题。
