# 工作总结：AGENTS 主线演进约束

## 本次补充

- 在仓库级 `AGENTS.md` 中新增 `Current Mainline And Evolution Constraints` 章节。
- 明确当前默认交付目标仍是《歼灭追击战》固定路线 MVP，禁止在无明确指示时把主链路改写为通用 YOLO 或 LLM 主链路。
- 固化中期双链路原则：已适配副本走 `ROI + 固定流程`，未适配副本走 `YOLO + 通用流程`。
- 明确静态 UI 元素优先使用固定 ROI / 规则法，动态空间目标优先使用 YOLO。
- 明确 LLM 只能位于高层决策层，不得直接输出底层键鼠动作，也不得绕过 `GuardLayer`、熔断和本地控制层。
- 明确架构演进采用外扩式重构，优先新增抽象和适配层，不推倒当前 DNF MVP 资产。
- 明确多副本扩展优先采用注册式入口，并要求文档区分“当前已实现事实”“中期规划”“长期预留”。

## 目的

- 把已在主线文档中统一的架构口径下沉到仓库协作规则，减少后续实现和文档再次偏离主线的风险。
- 为后续围绕 `FixedRoutePipeline`、`StateReader`、ROI 资产和固定路线资料的增量演进建立明确边界。

## 验证说明

- 本次提交仅涉及协作规范文档，不涉及运行时代码。
- 未执行测试；现有测试基线与历史失败情况保持不变。
